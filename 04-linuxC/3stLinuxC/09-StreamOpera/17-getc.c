/*******************************************************************
 *   > File Name: 17-getc.c
 *   > Author: fly
 *   > Mail: XXXXXXXX@icode.com
 *   > Create Time: Fri Jun 16 16:21:33 2017
 ******************************************************************/
#if (0)
int getc(FILE *stream);    //由文件中读取一个字符
//用来从参数 stream 所指的文件中读取一个字符。若读到文
//件尾而无数据时便返回 EOF。虽然 getc（）与 fgetc（）作用相同，
//但 getc（）为宏定义，非真正的函数调用。
//getc（）会返回读取到的字符，若返回 EOF 则表示到了文件尾。
#endif

#include <stdio.h>

int main(int argc, char* argv[])
{
    return 0;
}
